import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this to your production domain so OG image / sitemap URLs are absolute:
  // site: 'https://soloxtek.com',
});
